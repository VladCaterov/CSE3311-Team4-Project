using UnityEngine;

public class Boss : MonoBehaviour
{
   //public PlayerMovement Test;

    // Update is called once per frame
    void Update()
    {
        /*
        Test = GetComponent<PlayerMovement>();
        if (Test != null)
        {
            Test.markPoints(PointsMod); //Marks points off Josh's "score"
        }
        */
    }
}
